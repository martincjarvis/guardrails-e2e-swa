# contoso-notes

Azure Static Web App (TypeScript front-end) with a .NET Functions BFF,
deployed with azd + Bicep.

- `app/` — SWA front-end
- `api/` — Functions BFF (isolated worker)
- `infra/` — Bicep
